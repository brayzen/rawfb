h1 Login
form(action="/login" method="post")
	label(for="username" Username)
	input(name="username" type="text")
	br

	label(for="password" Password)
	input(name="password" type="password")
	br

	input(type="submit")

	p#signup-but Need to Sign up?  
		a(href="/signup") Yes?

